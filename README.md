# Personal website

Quarto website. Each page is a `.qmd` file (plain Markdown); the navbar is defined once in `_quarto.yml`. Preview locally with `quarto preview`, build with `quarto render`, deploy with `quarto publish gh-pages` once the GitHub repository exists.
